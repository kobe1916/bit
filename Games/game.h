#pragma once
#include <stdio.h>
#include <stdlib.h>

#define Row 9
#define Col 9

#define Rows Row+2
#define Cols Col+2

//函数的声明
InitMine(char a[Rows][Cols],int rows,int cols,char set);
Show(char a[Rows][Cols], int rows, int cols);
Show(char a[Rows][Cols], int rows, int cols);
