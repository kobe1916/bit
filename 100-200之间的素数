#include <stdio.h>
#include <math.h>

main()
{
  int i = 0;
  for (i = 100, i <= 200, i++)
  {
    int j = 100;
    for (j, j<= i,j++)
    {
      if(i%j != 0)
      printf("%d",i);
      }
      }
      }
    
